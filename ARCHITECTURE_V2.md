# NOTAM Korea V2.0 - 아키텍처 설계

## 📋 구현 목표
1. ✅ 3D 고도 시각화 (Deck.gl)
2. ✅ 모바일 앱 테스트 및 최적화
3. ✅ 푸시 알림 시스템
4. ✅ 다크 모드 전체 적용
5. ✅ 다국어 지원 (한국어/영어)
6. ✅ 통계 대시보드 확장

---

## 🎨 1. 3D 고도 시각화

### 기술 스택
- **Deck.gl** - WebGL 기반 3D 데이터 시각화
- **react-map-gl** - Mapbox GL JS 래퍼
- **@deck.gl/layers** - Column Layer (고도 표현)

### 구현 방식
```javascript
// 3D 원기둥 레이어로 NOTAM 고도 표시
import { ColumnLayer } from '@deck.gl/layers';

const layers = [
  new ColumnLayer({
    id: 'notam-3d-layer',
    data: notams,
    getPosition: d => [d.longitude, d.latitude],
    getElevation: d => parseFloat(d.altitude) * 0.3048, // feet to meters
    getFillColor: d => getColorByAltitude(d.altitude),
    radius: 500,
    elevationScale: 1,
  })
];
```

### UI 컴포넌트
```
┌─────────────────────────────────┐
│ [ 2D ] [ 3D ] ← 토글 버튼        │
├─────────────────────────────────┤
│                                 │
│   3D 지도 뷰 (pitch: 45°)       │
│   ┌───┐  ┌─────┐  ┌──┐         │
│   │ L │  │  M  │  │H │         │
│   └───┘  └─────┘  └──┘         │
│   낮음    중간     높음          │
│                                 │
│   🎚️ 고도 슬라이더 (필터)       │
│   └────●──────────────┘         │
│   0ft        5,000ft   10,000ft │
└─────────────────────────────────┘
```

### 파일 구조
```
src/
├── components/
│   ├── Map3DView.js          (3D 지도 컴포넌트)
│   ├── Map3DToggle.js        (2D/3D 토글)
│   └── AltitudeSlider.js     (고도 필터)
├── utils/
│   └── deckglLayers.js       (레이어 정의)
```

---

## 🔔 2. 푸시 알림 시스템

### 기술 스택
- **expo-notifications** - 크로스 플랫폼 알림
- **Firebase Cloud Messaging (FCM)** - 백엔드 전송
- **AWS SNS** (옵션) - 대량 전송

### 알림 트리거
1. **새 NOTAM 발행** - 즐겨찾기 공항
2. **중요도 높은 NOTAM** - Q-code 기반 (QMR, QWD 등)
3. **만료 임박** - 12시간 전
4. **NOTAM 변경** - 기존 NOTAM 업데이트

### 구현 플로우
```
┌─────────────┐
│ EC2 크롤러   │ → 새 NOTAM 발견
└──────┬──────┘
       │
       ▼
┌─────────────┐
│ 알림 서비스  │ → 즐겨찾기/필터 체크
└──────┬──────┘
       │
       ▼
┌─────────────┐
│ FCM/APNS    │ → 디바이스 전송
└──────┬──────┘
       │
       ▼
┌─────────────┐
│ 사용자 앱    │ → 알림 표시
└─────────────┘
```

### 파일 구조
```
backend/
├── notification_service.py   (알림 로직)
├── fcm_sender.py             (FCM 전송)
└── notam_watcher.py          (변경 감지)

frontend/
├── services/
│   ├── notificationService.js (클라이언트 처리)
│   └── pushTokenManager.js    (토큰 관리)
├── screens/
│   └── NotificationSettingsScreen.js
```

---

## 🌙 3. 다크 모드

### 테마 시스템
```javascript
// src/theme/colors.js
export const lightTheme = {
  background: '#F5F7FA',
  surface: '#FFFFFF',
  primary: '#1976D2',
  text: '#1A1A1A',
  textSecondary: '#666666',
  border: '#E0E0E0',
  // ... 30+ 색상
};

export const darkTheme = {
  background: '#121212',
  surface: '#1E1E1E',
  primary: '#64B5F6',
  text: '#FFFFFF',
  textSecondary: '#B0B0B0',
  border: '#333333',
  // ... 30+ 색상
};
```

### Context API
```javascript
// src/context/ThemeContext.js
const ThemeContext = createContext({
  theme: 'light',
  colors: lightTheme,
  toggleTheme: () => {},
});

export const ThemeProvider = ({ children }) => {
  const [theme, setTheme] = useState('light');

  useEffect(() => {
    // AsyncStorage에서 불러오기
    loadThemePreference();
  }, []);

  const toggleTheme = () => {
    const newTheme = theme === 'light' ? 'dark' : 'light';
    setTheme(newTheme);
    saveThemePreference(newTheme);
  };

  return (
    <ThemeContext.Provider value={{
      theme,
      colors: theme === 'light' ? lightTheme : darkTheme,
      toggleTheme,
    }}>
      {children}
    </ThemeContext.Provider>
  );
};
```

### 모든 스크린 적용
- ✅ DashboardScreen
- ✅ ModernNotamListScreen
- ✅ NotamDetailScreen
- ✅ ImprovedMapScreen
- ✅ SettingsScreen
- ✅ AirportDetailScreen

---

## 🌐 4. 다국어 지원 (i18n)

### 라이브러리
- **i18next** - 번역 엔진
- **react-i18next** - React 통합
- **expo-localization** - 디바이스 언어 감지

### 번역 파일 구조
```
src/
├── locales/
│   ├── ko/
│   │   ├── common.json
│   │   ├── notam.json
│   │   ├── map.json
│   │   └── settings.json
│   └── en/
│       ├── common.json
│       ├── notam.json
│       ├── map.json
│       └── settings.json
```

### 번역 키 예시
```json
// ko/notam.json
{
  "title": "NOTAM 목록",
  "search_placeholder": "NOTAM 검색...",
  "filter_all": "전체",
  "filter_active": "활성",
  "status_active": "활성",
  "status_expired": "만료",
  "altitude": "고도",
  "valid_from": "발효 시작",
  "valid_until": "발효 종료"
}

// en/notam.json
{
  "title": "NOTAM List",
  "search_placeholder": "Search NOTAMs...",
  "filter_all": "All",
  "filter_active": "Active",
  "status_active": "Active",
  "status_expired": "Expired",
  "altitude": "Altitude",
  "valid_from": "Valid From",
  "valid_until": "Valid Until"
}
```

### 사용 예시
```javascript
import { useTranslation } from 'react-i18next';

const NotamListScreen = () => {
  const { t } = useTranslation('notam');

  return (
    <Text>{t('title')}</Text>  // "NOTAM 목록" or "NOTAM List"
  );
};
```

---

## 📊 5. 통계 대시보드 확장

### 추가할 차트
1. **시간별 NOTAM 추이** (Line Chart)
2. **공항별 분포** (Bar Chart - 이미 있음, 확장)
3. **타입별 파이 차트** (Pie Chart)
4. **고도 분포 히스토그램** (Histogram)
5. **월별 발행 통계** (Calendar Heatmap)

### 라이브러리
- **react-native-chart-kit** - 모바일
- **recharts** - 웹

### 새로운 대시보드 카드
```
┌─────────────────────────────────────┐
│ 📈 NOTAM 발행 추이 (최근 30일)       │
│ ┌─────────────────────────────────┐ │
│ │     ╱╲    ╱╲                    │ │
│ │    ╱  ╲  ╱  ╲   ╱╲             │ │
│ │   ╱    ╲╱    ╲ ╱  ╲            │ │
│ └─────────────────────────────────┘ │
└─────────────────────────────────────┘

┌─────────────────────────────────────┐
│ 🥧 NOTAM 타입 분포                   │
│ ┌─────────────────────────────────┐ │
│ │   QMR (활주로) ████████ 35%     │ │
│ │   QWD (위험)   ██████   25%     │ │
│ │   QOB (장애물) ████     15%     │ │
│ │   기타         ██████   25%     │ │
│ └─────────────────────────────────┘ │
└─────────────────────────────────────┘

┌─────────────────────────────────────┐
│ 🗺️ 고도 분포                        │
│ ┌─────────────────────────────────┐ │
│ │ ┃                               │ │
│ │ ┃  ┃                            │ │
│ │ ┃  ┃  ┃  ┃                      │ │
│ │ ┃  ┃  ┃  ┃  ┃  ┃  ┃           │ │
│ │ 0  3k 6k 9k 12k 15k 18k ft     │ │
│ └─────────────────────────────────┘ │
└─────────────────────────────────────┘
```

---

## 📱 6. 모바일 앱 최적화

### 성능 개선
1. **FlatList 가상화** - windowSize, maxToRenderPerBatch
2. **이미지 최적화** - Fast Image 사용
3. **메모이제이션** - useMemo, useCallback
4. **코드 스플리팅** - React.lazy

### 테스트 계획
```
✅ Android (실기기)
  - Galaxy S21 (Android 13)
  - Pixel 6 (Android 14)

✅ iOS (시뮬레이터)
  - iPhone 14 Pro (iOS 17)
  - iPad Pro (iOS 17)

✅ 웹 브라우저
  - Chrome (desktop/mobile)
  - Safari (desktop/mobile)
  - Firefox
```

### 성능 메트릭
- Time to Interactive (TTI) < 3s
- First Contentful Paint (FCP) < 1.5s
- Frame Rate > 55 FPS (지도 렌더링)

---

## 🎯 구현 순서

### Phase 1: 기반 작업 (1-2시간)
1. ✅ 다크 모드 테마 시스템 구축
2. ✅ i18n 설정 및 번역 파일 생성
3. ✅ 새로운 의존성 설치

### Phase 2: UI 개선 (2-3시간)
4. ✅ 모든 스크린에 다크 모드 적용
5. ✅ 모든 텍스트 번역 적용
6. ✅ 통계 차트 추가

### Phase 3: 고급 기능 (3-4시간)
7. ✅ 3D 지도 구현
8. ✅ 푸시 알림 설정
9. ✅ 모바일 성능 최적화

### Phase 4: 테스트 및 배포 (1-2시간)
10. ✅ 전체 기능 테스트
11. ✅ 버그 수정
12. ✅ 문서 업데이트

---

## 📦 새로운 의존성

```json
{
  "dependencies": {
    "@deck.gl/core": "^9.0.0",
    "@deck.gl/layers": "^9.0.0",
    "@deck.gl/react": "^9.0.0",
    "react-map-gl": "^7.1.0",
    "mapbox-gl": "^3.0.0",
    "i18next": "^23.7.0",
    "react-i18next": "^14.0.0",
    "expo-localization": "~15.0.0",
    "expo-notifications": "~0.27.0",
    "react-native-chart-kit": "^6.12.0",
    "recharts": "^2.10.0",
    "react-native-svg": "~15.6.0"
  }
}
```

---

## 🔐 환경 변수 추가

```env
# .env
MAPBOX_ACCESS_TOKEN=pk.eyJ1...
FCM_SERVER_KEY=AAAA...
```

---

**작성일**: 2025-11-15
**버전**: 2.0.0-alpha
**담당**: Claude Code Multi-Agent System
